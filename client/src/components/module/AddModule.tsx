import React, { useContext } from 'react'
import { ControlContext } from '../../hooks/ControlContext'

const AddModule = () => {
    const { showAddModule, hideAddModule } = useContext(ControlContext);
    return (
        <div>
            
        </div>
    )
}

export default AddModule
