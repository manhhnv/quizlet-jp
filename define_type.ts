type windowState = "open" | "closed" | "minimized"

const test: windowState = "closed"
console.log(test)

import { User } from './interface'

const b: User = {
    name: 'ABC',
    id: 10
}
console.log(b)
