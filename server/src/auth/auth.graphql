input LoginInput {
  email: String!
  password: String!
}
type UserToken {
  token: String!
  user: User!
}

input RegisterInput {
  name: String!
  email: String!
  birthday: String!
  password: String!
}

type Mutation {
  register(input: RegisterInput!): UserToken
  login(input: LoginInput): UserToken
}
