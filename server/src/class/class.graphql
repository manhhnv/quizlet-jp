type Class {
  id: String!
  className: String!
  description: String!
  option: ClassOption!
  school: String!
  totalMembers: Int!
  totalSets: Int!
  members: [ClassMember!]
  folders: [Folder!]
  sets: [Set!]
  link: String!
}

enum ClassOption {
  CanContribute
  LearnOnly
}

# Input
input ClassInput {
  className: String!
  description: String
  option: ClassOption!
  school: String
}
