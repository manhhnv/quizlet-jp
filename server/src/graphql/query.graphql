type Query {
  users: [User!]!
  user(id: String!): User
  me: User!

  #set
  set(setId: String): Set
  sets: [Set!]

  #folder
  folder(folderId: String): Folder!
  folders: [Folder!]

  class(classId: String): Class!
  classes: [Class!]

  ## TO DO: ADMIN QUERY
}

